# Contributing Guide

> **🤖 AI 코딩 도구(Claude Code, Cursor, Copilot Chat 등)를 사용한다면, 작업 시작 전에 이 파일을 시스템 프롬프트(또는 그에 준하는 영구 컨텍스트)에 반드시 등록할 것.**
> 이 가이드를 모르면 AI는 한국어 정책을 어기고 영어·중국어 커밋 메시지나 주석을 자유롭게 생성한다. 본 파일을 먼저 읽도록 강제해야 정책이 일관되게 유지된다.
> - 각 도구의 "Rules", "Custom Instructions", "System Prompt" 영역에 본 파일 경로/내용을 등록.
> - 세션이 새로 열릴 때마다 다시 로드해야 하는 도구가 대부분이니 매번 확인.

---

## 0. 언어 정책

본 프로젝트의 모든 산출물은 **한국어**가 기본이다.

### 한국어로 작성

- 커밋 메시지 (제목·본문)
- 브랜치 이름의 설명 부분
- PR 제목·본문
- 이슈 / PR 코멘트
- 소스 코드 주석
- 로그·예외 메시지(비대면)
- TODO / FIXME

### 비한국어권 팀원 (예: 중국어 모어 팀원)

- 작성은 모어로 하되, **레포에 푸시·머지하기 전에 반드시 한국어로 변환해서 올린다.**
- 변환 도구는 자유 (ChatGPT, DeepL, Claude 등). 한국어 검수는 본인 책임.
- 변환 결과가 의미상 모호하면 PR 본문에 원문(영어 또는 중국어)을 함께 적어두면 한국인 리뷰어가 빠르게 교정 가능.
- 로컬 작업 중인 임시 주석·메모는 모어로 두어도 무방. 커밋에 들어가는 순간부터 한국어.

### 사용자 대면 문자열

API 응답 메시지, i18n 리소스 등 **end user에게 노출되는 문자열**은 본 정책과 별개로 제품 요구사항(현재는 한국어 단일)을 따른다.

### 실무 룰

- 정책 위반 텍스트를 발견하면 작성자에게 알리고 수정 요청. 자동 차단(CI lint)은 현재 없음.
- 식별자(클래스·메서드·변수 이름)는 영어. 한국어 식별자 금지.

---

## 1. 브랜치 전략

`main` 브랜치를 기반으로 작업 브랜치를 생성하고 PR로 병합한다. `main` 직접 푸시는 레포 룰로 차단되어 있다.

```
main
├── feat/login
├── feat/study-room
└── fix/session-timeout
```

### 브랜치 네이밍

```
<type>/<짧은-영문-설명>
```

| type       | 의미                       |
|------------|----------------------------|
| `feat`     | 새 기능                    |
| `fix`      | 버그 수정                  |
| `refactor` | 리팩토링                   |
| `docs`     | 문서만 수정                |
| `chore`    | 빌드·설정·툴링 등          |

**예시**

```
feat/oauth-login
fix/signup-validation
docs/api-guide
```

> 브랜치명 뒤쪽의 짧은 설명만 영어 케밥케이스로 적는다. 한글 브랜치명은 도구·CLI 호환 문제로 금지.

---

## 2. 커밋 컨벤션

```
<type>: <제목 (한국어)>
```

- 제목은 **현재형 동사**로 시작 — "추가", "수정", "제거", "리팩토링" 등
- 제목은 **70자 이내**
- 본문(선택)은 **왜** 바꿨는지 위주. 무엇을 바꿨는지는 diff가 말해준다.

**예시**

```
feat: 구글 OAuth 로그인 추가
fix: 비밀번호 유효성 검사 정규식 수정
docs: 스터디룸 API 명세 갱신
refactor: 세션 토큰 로직을 서비스로 분리
```

---

## 3. PR 규칙

- 항상 최신 `main`에서 브랜치 생성.
- PR 제목은 커밋 컨벤션과 동일한 형식.
- PR 본문은 변경 사유·영향 범위 위주로 작성.
- 머지 후 작업 브랜치는 삭제.

---

## 4. 코드 주석

- **기본은 주석을 쓰지 않는다.** 잘 지은 식별자·함수명이 1차 문서.
- 주석을 쓰는 경우: 코드만 봐서는 알 수 없는 **이유(WHY)** — 숨은 제약, 워크어라운드 사유, 비자명한 invariant.
- 코드가 무엇을 하는지(WHAT)는 쓰지 않는다.
- 주석은 **개조식** (한국어 CLAUDE 룰과 일치). 장황한 문장 대신 짧은 항목 나열.
- 모든 주석은 한국어. (비한국어권 팀원은 §0 참조 — 푸시 전 변환.)
- `TODO:` / `FIXME:` 는 가능하면 이슈 번호와 함께. 예: `// TODO(#42): 만료된 토큰 처리`.

---

## 5. 테스트 주도 개발 (TDD)

**모든 새 기능은 테스트 우선(test-first)으로 개발한다.** 회원/인증 모듈이 이렇게 만들어졌고, 이후 추가되는 모든 기능에도 동일하게 적용된다.

### 기능 단위 워크플로

1. **Red** — 추가할 동작을 못 박는 실패하는 테스트부터 작성. 실행해서 "올바른 이유로" 실패하는지 확인.
2. **Green** — 테스트를 통과시키는 **최소한의** 프로덕션 코드만 작성. 테스트가 요구하지 않는 것은 넣지 않는다.
3. **Refactor** — 스위트가 초록불인 상태에서 프로덕션·테스트 코드를 정리. 의미 있는 변경 후마다 다시 돌린다.

### 테스트 레이어

- **Unit 테스트** — 순수 로직 (서비스, 검증기, 도메인 객체). 협력 객체는 mock. 빠르고, 스위트의 대부분을 차지.
- **Slice 테스트** — `@WebMvcTest`, `@DataJpaTest` 등 전체 컨텍스트를 띄우지 않고 프레임워크 연동만 검증.
- **Integration 테스트** — `@SpringBootTest` + H2 프로필. 보안 필터 체인·트랜잭션 경계 같은 cross-cutting 동작의 해피패스 정도. 적게, 의미 있게.

### 룰

- 테스트 없이 기능을 커밋하지 않는다. "나중에 추가"는 안정적으로 "추가 안 함"이 된다.
- 테스트 이름은 동작 기반. `signUp_rejectsDuplicateEmail` > `testSignUp1`.
- 한 테스트는 하나의 동작만 단언. "그리고"가 들어가면 분리.
- 테스트 간 가변 상태 공유 금지. 각자 필요한 셋업을 직접.
- 버그 수정은 **회귀 테스트 먼저** — 버그를 재현하는 실패 테스트를 짠 다음 수정. 수정 전 실패, 수정 후 통과를 확인.
- 커버리지는 신호일 뿐 목표가 아니다. 숫자 올리기 위한 테스트는 쓰지 않는다.

### 테스트가 필요 없는 것

- 단순 getter/setter, Lombok 생성 코드, 로직 없는 설정 클래스.
- 일회성 스크립트·프로토타입 (명확히 표시할 것).

---

## 6. DB·아키텍처 룰

- **DB 트리거 금지.**
  - `studymate_schema.sql` / `tmp(2).sql` 의 트리거·가드 테이블은 무시.
  - 동일한 비즈니스 규칙은 **서비스 레이어의 트랜잭션 + 락**으로 구현.
- 스키마 변경은 마이그레이션 스크립트로. prod DB 직접 수정 금지.

---

## 7. 문서 우선순위 (Source of Truth)

문서 간 내용이 충돌할 때 신뢰 순서:

1. 프로젝트 결정 로그의 확정 항목 (`D-NNN`)
2. **Swagger / OpenAPI** — 코드에서 자동 생성되므로 API 표면에 관한 한 최고 진실원
3. API 명세 시트 — Swagger 미생성 시점에만 권위
4. SRS (요구사항정의서)
5. SDP (개발계획서)
6. SQL 스키마 (단, 트리거는 §6에 의해 제외)
7. 작업 시트 비고

위 순서로 충돌이 해소되지 않으면 팀 채널에 질문 → 결정되면 결정 로그에 `D-NNN`(확정) 또는 `P-NNN`(미해결)로 기록.

---

## 8. 로컬 환경 셋업

자세한 내용은 [`backend/README.md`](backend/README.md) 참조.

- **시크릿 커밋 금지** (DB 비밀번호, OAuth 키, JWT 시크릿, 메일 자격증명). 로컬은 `application-local.yml` 또는 환경 변수.
- `/actuator/health` 는 의도적으로 인증 예외. 로드밸런서 헬스체크용. 인증으로 막지 말 것.

---

## 9. AI 도구 사용 시 추가 유의사항

- AI가 생성한 커밋 메시지·주석이 한국어 정책을 어기지 않는지 push 전 확인.
- `git diff` 로 무관한 파일이 수정·삭제되지 않았는지 점검 후 커밋.
- AI에게 작업을 시킬 때 본 `CONTRIBUTING.md` 와 관련 `docs/` 파일을 컨텍스트로 제공.
